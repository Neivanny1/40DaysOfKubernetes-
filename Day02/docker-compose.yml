version: '3.4'

services:
  101app:
    image: 101app
    build:
      context: getting-started-app
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
