{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/getting-started-app/package.json",
                "localRoot": "${workspaceFolder}/getting-started-app"
            }
        }
    ]
}